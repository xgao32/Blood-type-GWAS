The `sg10k` directory contains relevant files for the SG10K project.

## Folders
1. `aggregate_vcf` contains aggregate data (no sample level data) in `vcf.bgz` and corresponding `tbi` files.


## Files
1. `00_download_sg10k.sh` script to download VCF files from the CHORUS browser that is used to access SG10K data but failed.  
2. `00_validate_vcf_files.pbs` script to verify validity of VCF files used to submit job to NUS HPC. Failed to verify for some unknown reason, VCF files look valid.
3. `01_validate_vcf_files.pbs` same as (2) and also failed.